# 核心概念概述

OpenKB 是一个面向 AI Agent 与团队维护者的版本化持久项目知识库系统。

## 核心能力
1. **统一架构**：一个基于 Fastify 的 Node.js/TypeScript 应用，同时承载 Web 控制台和原生 MCP 接口。
2. **知识粒度**：支持全局规范 (`@global`) 与指定项目规约。
3. **提案审核**：AI Agent 学习或修改知识时可以发起变更提案，由团队成员在控制台中审核通过后正式发布。
